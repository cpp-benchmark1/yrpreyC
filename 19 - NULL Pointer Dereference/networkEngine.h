#ifndef NETWORK_ENGINE_H
#define NETWORK_ENGINE_H

#include <string>

namespace networkEngine {
    /// Network processing engine for handling network operations
    /// Processes network requests and performs network operations
    int processNetworkOperations(const std::string& network_data);
}

#endif
