#ifndef BIGNUM_HANDLER_H
#define BIGNUM_HANDLER_H

/// Handler for processing BIGNUM operations
/// Receives BIGNUM data via recvmsg and processes it through BIGNUM operations
int bignumHandler_processBignumStream(void);

#endif
