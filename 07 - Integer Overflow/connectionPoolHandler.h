#ifndef CONNECTION_POOL_HANDLER_H
#define CONNECTION_POOL_HANDLER_H

/// Handler for processing connection pool operations
/// Receives connection data via UDP and processes it through connection pool operations
int connectionPoolHandler_processConnectionPoolStream(void);

#endif
